# 后台设置中心与运营看板升级计划书

生成日期：2026-04-08

约束依据：
- 根目录 `agents.md`
- 现有实现：`frontend/src/content/copy/`、`frontend/src/components/admin/`、`backend/biz/service/site_settings.go`、`backend/biz/service/dashboard.go`
- 本计划按 `frontend-design` skill 的信息架构思路整理，但严格保留你仓库现有的亮色、液态玻璃、Astro + Vue Islands + Nano Stores、Go + Hertz + sqlc 约束

## 1. 计划目标

本轮升级不追求“把所有文案都搬到后台”，而是聚焦三件事：

1. 把当前分散的页尾设置、站点资料、个人设置升级为一个更完整的“后台设置中心”。
2. 把真正适合运营频繁调整的前台展示内容，从 `frontend/src/content/copy/` 迁移到后台可配置。
3. 在现有统计面板基础上，继续补齐外部依赖观测、内容运营统计和异常提示能力。

## 2. 当前现状判断

基于当前仓库代码，后台已经具备一部分设置与运营基础，但还没有形成完整的“可运营配置系统”。

| 维度 | 当前状态 | 结论 |
| --- | --- | --- |
| 文案入口 | 可见文案已较好地集中在 `frontend/src/content/copy/` | 适合做默认值和开发期维护，但不适合高频运营项 |
| 后台设置 | `site_settings` 表已存在，当前只落了 `site_profile` 与 `footer` 两类配置 | 后端结构可继续扩展，但需要先定义配置边界 |
| 后台页面 | 已有仪表盘、文章、说说、评论、友链、页尾、个人设置、备份 | 功能齐了，但“设置中心”还比较分散 |
| 统计看板 | 已有 analytics 概览、来源、环境、地域、热力、审计记录 | 更像访问分析，不够像运营驾驶舱 |
| 外部依赖 | 已接入 GitHub、天气、GeoIP、友链健康检查 | 缺少统一的后台状态可视化和可配置入口 |

## 3. 核心判断：不是所有 copy 都该进后台

你现在的 `copy/` 体系是对的，不建议一刀切废掉。更合理的做法，是把文本分成三层。

### 3.1 三层治理模型

| 层级 | 存放位置 | 适合内容 | 典型例子 |
| --- | --- | --- | --- |
| 静态默认文案 | `frontend/src/content/copy/` | 系统文案、按钮文案、校验提示、aria、低频说明、默认 fallback | 后台按钮名、提示语、空态文案、组件说明 |
| 站点运营配置 | `site_settings` + 后台设置中心 | 站点展示内容、首页配置、作者展示、可频繁调整的介绍性文案 | `heroTitle`、`heroSubtitle`、首页图片、社交链接 |
| 内容实体数据 | `posts` / `moments` / `friends` 等业务表 | 文章、说说、评论、友链申请等真实内容 | 博文正文、说说列表、友链卡片 |

### 3.2 第一批应迁移到后台的内容

这一批最符合你说的“源码里改不太合适，后台改更好”。

| 模块 | 建议迁移字段 | 原因 |
| --- | --- | --- |
| 首页 Hero | `home.heroTitle`、`home.heroSubtitle` | 高频展示项，属于站点门面，改源码成本太高 |
| 首页视觉资源 | `home.heroImages` | 适合后台换封面图，不应每次改代码 |
| 站点品牌资料 | `brand.text`、`brand.logoAlt`、`seo.siteTitle`、`seo.siteUrl`、`seo.defaultDescription`、`seo.defaultSocialImage` | 当前已部分后台化，应继续作为设置中心正式模块 |
| 页尾合规信息 | `siteFooter.defaults` 对应字段 | 当前已后台化，应并入统一设置中心 |
| 作者展示资料 | 首页作者简介、社交链接、GitHub 用户名、About 页展示性资料 | 这类内容带明显运营属性，不适合长期写死在 `copy` |
| 首页轻运营模块 | `blogIndex.quickStats`、`nowItems`、`playlist` 这类“阶段性展示内容” | 更新频率高，后续很可能要经常改 |

### 3.3 建议继续留在 `copy/` 的内容

以下内容不建议进后台，否则设置中心会很快变成“什么都能改，但什么都难管”的大杂烩。

- 后台系统按钮文案、toast、表单校验提示
- aria-label、可访问性说明、技术性占位文案
- 组件级 loading / empty / error 文案
- 状态枚举映射，如“已发布 / 草稿 / 待审核”
- 带明显交互语义、但不属于运营内容的提示文案

## 4. 设置中心目标形态

### 4.1 导航结构建议

不建议一上来把侧边栏拆成很多新页面。第一阶段更稳妥的方式，是新增一个聚合式的 `/admin/settings` 页面，用分组 Tabs 或分组卡片承载设置项。

建议侧边栏最终结构如下：

| 分组 | 页面 |
| --- | --- |
| Overview | 仪表盘 |
| Content | 文章管理、说说管理、评论审核、友链管理 |
| Settings | 设置中心 |
| System | 个人设置、数据备份 |

说明：
- 现有 `/admin/footer`、`/admin/profile` 在过渡期可保留。
- 当前已完成最终收口，统一使用 `/admin/settings` 的对应分组，不再保留旧入口。

### 4.2 设置中心内部分组建议

建议设置中心按“内容职责”分，而不是按“技术实现”分。

| 分组 | 内容 |
| --- | --- |
| 站点资料 | 品牌名、站点标题、域名、默认描述、默认分享图 |
| 首页展示 | `heroTitle`、`heroSubtitle`、首页封面图、首页展示开关 |
| 作者与社交 | 作者昵称、头像、简介、社交链接、GitHub 用户名 |
| 页尾与合规 | ICP、页尾自定义文案、合规提示 |
| 集成与服务 | 天气、GitHub、友链健康检查、GeoIP 状态、前台组件开关 |
| 预览与发布 | 最近更新时间、最近修改人、恢复默认、发布状态提示 |

### 4.3 后台界面表现建议

为了符合当前项目风格与 `frontend-design` skill 的要求，设置中心建议使用以下 UI 模式：

- 亮色基调，不引入暗黑背景
- 延续液态玻璃卡片，但保持后台可读性优先
- 左侧为分组切换，中间为表单区，右侧为实时预览区
- 页面底部固定“保存 / 恢复默认 / 最近保存状态”操作条
- 每个配置块都展示“影响范围”，避免改完不知道作用到哪里
- 对首页 Hero、站点品牌、页尾等关键模块提供即时预览

## 5. 数据模型建议

### 5.1 延续 `site_settings`，但坚持“一类配置一个 key”

当前 `site_settings` 设计是 `key + jsonb`，这个方向可继续用，但不要把所有设置都塞进一个巨型 JSON。

建议新增或规范如下 key：

| key | 作用 |
| --- | --- |
| `site_profile` | 站点品牌与默认 SEO，延续现有实现 |
| `site_footer` 或继续沿用 `footer` | 页尾与备案信息 |
| `home_hero` | 首页 Hero 标题、副标题、展示开关 |
| `home_assets` | 首页图片资源、轮播策略、入口展示控制 |
| `author_profile` | 作者展示信息、社交链接、公开简介 |
| `site_integrations` | GitHub 用户名、天气地点、组件开关、依赖状态展示偏好 |
| `dashboard_preferences` | 仪表盘默认时间窗、默认排序、卡片开关 |

建议原则：

- 一个 key 只负责一个业务域
- 每个 key 都有默认值与 normalize 逻辑
- 每次更新都写入审计日志
- 未配置时永远回退到 `copy/` 默认值

### 5.2 安全边界

以下配置不能进入前端源码，也不建议做成后台明文可编辑：

- GitHub Token
- 任何第三方 API Secret
- JWT Secret
- 数据库与 Redis 凭证

建议规则：

- 后台只编辑“非敏感配置”，例如 GitHub 用户名、天气展示城市、前台模块开关
- 真正的 Secret 仍通过 `.env` 注入后端
- 设置中心只显示“已配置 / 未配置 / 最近成功时间”，不回显敏感值

## 6. 前后端落地方式

### 6.1 前端策略

遵守 `agents.md` 现有约束，前端仍采用：

- Astro 负责页面与 SSG
- Vue 3 负责后台互动表单和局部前台配置岛屿
- Nano Stores 负责跨页面共享配置态

建议新增的 store：

- `frontend/src/stores/homeHero.ts`
- `frontend/src/stores/homeAssets.ts`
- `frontend/src/stores/authorProfile.ts`
- `frontend/src/stores/siteIntegrations.ts`

这些 store 的角色与 `siteProfile.ts`、`siteFooter.ts` 保持一致：

1. 先从 `copy/` 取默认值
2. 再向后端拉取真实配置
3. 保存后写回 store
4. 前台组件消费 store 或运行时同步结果

### 6.2 首页 Hero 的特殊处理

`heroTitle` 和 `heroSubtitle` 与普通文案不同，它们直接影响首页首屏，不适合做成“页面加载后闪一下再替换”。

建议实现策略：

- `copy/` 保留默认值，确保 SSG 首屏有稳定回退
- 首页 Hero 在客户端激活前，优先读取上次缓存配置或尽早拉取 `home_hero`
- `HeroTitle` / `TypewriterSubtitle` 只在配置准备好后启动动画
- 保存配置后更新本地缓存，减少下次首屏闪动

这能兼顾 Astro 静态输出与后台即时可运营之间的矛盾。

## 7. 统计看板升级方向

当前后台仪表盘已经有“访问分析”，下一步应补成“运营驾驶舱”。

### 7.1 建议新增的看板模块

| 模块 | 建议内容 |
| --- | --- |
| 内容表现 | Top 文章 UV / PV / 点赞 / 评论 / 增长率 |
| 运营节奏 | 最近发布文章、定时发布队列、草稿积压、说说活跃度 |
| 审核效率 | 待审核数量、平均处理时长、驳回率、近 7 日处理趋势 |
| 转化线索 | 评论提交率、点赞率、留言活跃度、友链申请转化 |
| 异常提示 | 访问骤降、待审核积压、草稿长期未发布、依赖抓取失败 |

### 7.2 图表表现约束

继续统一使用 Apache ECharts，并保持：

- 主色使用 `#39c5bb`
- 对比色使用 `#c084fc`
- 亮色背景下的高对比度文本
- 卡片级液态玻璃，但图表区本体以可读性优先

## 8. 外部依赖运营能力补全方向

当前仓库里已经存在这些外部依赖或外部数据来源：

- GitHub Profile
- wttr.in 天气
- GeoIP 地理库
- 友链健康检查任务

下一步不只是“接上接口”，而是要让后台知道这些依赖是否健康。

### 8.1 建议新增“集成与服务”面板

| 服务 | 建议展示项 |
| --- | --- |
| GitHub | 用户名、缓存 TTL、最近成功时间、最近失败信息、手动刷新 |
| 天气 | 展示地点、缓存 TTL、最近成功时间、最近失败信息、前台开关 |
| GeoIP | 数据库是否可用、最近更新时间、降级状态 |
| 友链巡检 | 上次巡检时间、成功数、失败数、异常站点数 |

### 8.2 可配置项与不可配置项边界

可进后台的：

- GitHub 用户名
- 天气展示地点
- 首页是否展示天气 / 音乐 / 时钟
- 友链巡检结果显示方式

不进后台的：

- Token、API Secret、数据库路径、服务密钥

## 9. 推荐实施顺序

### Phase 1：设置中心骨架

目标：
- 新增 `/admin/settings`
- 把现有“页尾设置 + 站点资料”并入同一个设置中心界面
- 首轮保留旧入口，后续在 Phase 5 完成统一清理

交付：
- 新设置中心页面
- 基础分组导航
- 统一保存状态与恢复默认交互

### Phase 2：首页 Hero 与首页资源后台化

目标：
- 把 `heroTitle`、`heroSubtitle`、`heroImages` 正式接入后台

交付：
- `home_hero`、`home_assets` 配置结构
- 后台编辑表单
- 首页运行时读取与预览闭环

### Phase 3：作者资料与外部依赖设置

目标：
- 把作者展示资料、GitHub 用户名、天气地点、首页组件开关后台化

交付：
- `author_profile`、`site_integrations`
- About 与首页相关区域接入新配置
- 集成状态卡片

### Phase 4：运营仪表盘升级

目标：
- 从“访问统计”升级为“内容运营 + 依赖健康 + 异常提醒”仪表盘

交付：
- 内容表现卡片
- 审核效率模块
- 依赖健康模块
- 异常提示模块

### Phase 5：收口与治理

目标：
- 统一 fallback 策略
- 明确哪些文案禁止继续往后台迁
- 补齐测试与审计记录

交付：
- 设置项字典文档
- store / service / handler 测试补充
- 老入口清理或重定向

## 10. 验收标准

完成后，至少应满足以下标准：

- 首页 `heroTitle`、`heroSubtitle` 不再需要改源码即可更新
- `copy/` 仍然存在，并继续承担默认值与系统文案职责
- 后台设置中心成为站点展示配置的统一入口，而不是多个零散页面
- 外部依赖状态在后台可见，可区分“已配置、可用、降级、失败”
- 仪表盘不只展示流量，还能回答“哪些内容表现好、哪些依赖有问题、当前有哪些待处理事项”
- 敏感信息仍只通过环境变量管理，不进入前端源码或后台明文表单

## 11. 最终建议

这次升级最重要的不是“多做几个设置页”，而是先把边界定清楚：

- `copy/` 继续负责默认值和系统文案
- `site_settings` 负责站点运营配置
- 业务表继续负责真实内容数据

如果这个边界先定下来，后面你再扩首页、About、友链、外部服务、统计面板，就不会越做越乱，后台也会真正从“管理页面集合”升级为“运营中台入口”。

## 12. 实施进度记录

### 2026-04-08

已完成：

- [x] Phase 1 设置中心骨架已落地，新增 `/admin/settings`
- [x] 后台侧边栏新增“设置中心”入口，旧的 `/admin/footer` 与 `/admin/profile` 在该阶段先保留
- [x] 已将“站点资料 / 页尾与合规 / 个人与账号”整合到同一设置中心界面
- [x] 设置中心已补齐左侧分组切换、中间表单区、右侧实时预览、底部统一保存状态与恢复默认操作条
- [x] 继续复用现有 `siteProfile`、`siteFooter`、`auth` 能力，未破坏 `copy/` 默认值回退链路
- [x] Phase 2 首页 Hero 与首页资源后台化已落地，新增 `home_hero` / `home_assets` 配置链路
- [x] 设置中心已新增“首页 Hero / 首页视觉资源”分组、表单与预览
- [x] 首页 Hero 标题、副标题、背景图已改为“`copy/` 默认值 + 运行时 store 覆盖 + 本地缓存”模式
- [x] `HeroTitle`、`TypewriterSubtitle`、`HeroParallax`、`HeroShuffleBtn` 已适配后台配置更新
- [x] Phase 3 作者资料与外部依赖后台化已落地，新增 `author_profile` / `site_integrations` 配置链路
- [x] 设置中心已新增“作者展示资料 / 外部依赖与组件”分组、表单、实时预览与集成状态卡片
- [x] Blog 侧栏、About 首屏、About 联系区已切换到“`copy/` 默认值 + 运行时 store 覆盖”模式
- [x] 首页顶部天气地点、音乐播放器、系统时钟已接入后台开关与运行时配置
- [x] About GitHub 模块已改为读取后台 GitHub 用户名并支持动态刷新
- [x] 已再次完成验证：`cd backend && go test ./...`
- [x] 已再次完成验证：`cd backend && go build ./...`
- [x] 已再次完成验证：`cd frontend && npm run build`

### 2026-04-09

已完成：

- [x] Phase 4 运营仪表盘升级已按 A 方案落地，在保留原有访问分析面板的前提下新增“运营驾驶舱”层
- [x] 仪表盘已新增内容表现模块，可按阅读 / 点赞 / 评论综合热度查看 Top 文章
- [x] 仪表盘已新增运营节奏模块，可查看最近发布、定时文章、长期草稿与近 7 天说说活跃度
- [x] 仪表盘已新增审核压力模块，先以待审核规模与最近处理动作替代迁移型“审核效率”方案
- [x] 仪表盘已新增依赖健康模块，可统一观察 GitHub、天气、GeoIP、友链巡检与首页组件开关
- [x] 仪表盘已新增异常提醒模块，可提示访问波动、审核积压、长期草稿与外部依赖异常
- [x] Phase 4 继续复用现有接口与 `copy/` 文案体系，未引入额外数据库迁移
- [x] Phase 5 收口与治理已完成，新增设置项字典文档 `ADMIN_SETTINGS_CENTER_SETTINGS_DICTIONARY.md`
- [x] 已明确 `copy/`、`site_settings`、业务表三层边界，并补齐“禁止继续往后台迁移”的内容范围
- [x] 设置中心已支持 `?section=` 分组定位，旧的 `/admin/footer`、`/admin/profile` 已完成下线
- [x] 后台侧边栏已收口为统一“设置中心”入口，旧入口不再作为日常主导航项
- [x] 设置中心页内的旧入口兼容提示与快捷链接已移除，后台只保留统一设置入口
- [x] 已补齐 `site_settings` 相关 service / handler 单元测试，覆盖 public/admin 基础成功与错误分支
- [x] 已完成验证：`cd backend && go test ./...`
- [x] 已完成验证：`cd backend && go build ./...`
- [x] 已完成验证：`cd frontend && npm run build`
