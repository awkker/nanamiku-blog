# Server
APP_ENV=development
SERVER_PORT=8080

# PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_USER=miku
DB_PASSWORD=miku_secret
DB_NAME=miku_blog
DB_SSLMODE=disable

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=miku_redis
REDIS_DB=0

# JWT
JWT_SECRET=change-me-in-production
JWT_ACCESS_TTL=15m
JWT_REFRESH_TTL=720h

# Session cookies
COOKIE_DOMAIN=
COOKIE_SECURE=false
COOKIE_SAME_SITE=lax

# CORS
CORS_ORIGINS=http://localhost:4321,http://localhost:3000

# GeoIP (optional, for analytics location fallback)
# Example: /absolute/path/to/GeoLite2-City.mmdb
GEOIP_DB_PATH=
